# ─── Build tools (must come first to fix pkg_resources errors) ─
setuptools>=69.5.1
wheel>=0.43.0

# ─── Web Framework ────────────────────────────────────────────
flask==3.0.3
flask-cors==4.0.1
python-dotenv==1.0.1
gunicorn==22.0.0

# ─── IBM watsonx.ai SDK ───────────────────────────────────────
# 1.3.40+ has no Python upper cap; ibm-cloud-sdk-core 3.22.0 ships a wheel
ibm-watsonx-ai==1.3.40
ibm-cloud-sdk-core==3.22.0

# ─── RAG / Vector Store ───────────────────────────────────────
# langchain 0.3.x dropped the numpy<2 restriction
langchain==0.3.11
langchain-community==0.3.11
langchain-text-splitters==0.3.3
faiss-cpu==1.9.0.post1
# sentence-transformers 3.3.1 + transformers 4.46.3: stable combo before
# the tf-keras/Keras-3 breakage introduced in transformers 4.47+
sentence-transformers==3.3.1
transformers==4.46.3

# ─── PDF Processing ───────────────────────────────────────────
pypdf==4.3.1
pdfplumber==0.11.4

# ─── Utilities ────────────────────────────────────────────────
# No explicit numpy pin — let dependencies resolve it freely
requests==2.32.3
# tf-keras is the official shim required when Keras 3 is installed in
# the Anaconda base environment (transformers TF backend triggers this)
tf-keras>=2.16.0
